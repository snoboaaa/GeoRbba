# Root conftest.py for pytest monorepo compatibility
# Empty file to mark the root as the pytest rootdir
# Each app's tests/ directory is a namespace package (no __init__.py)
# so they get unique module names like apps.core.tests, apps.geoapi.tests

